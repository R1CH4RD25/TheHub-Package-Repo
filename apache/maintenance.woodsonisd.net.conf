<VirtualHost *:80>
    ServerName maintenance.woodsonisd.net
    ServerAdmin richard.sullivan@woodsonisd.net

    DocumentRoot /var/www/woodson/maintenance/public

    <Directory /var/www/woodson/maintenance/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Logging
    ErrorLog ${APACHE_LOG_DIR}/maintenance.woodsonisd.net-error.log
    CustomLog ${APACHE_LOG_DIR}/maintenance.woodsonisd.net-access.log combined

    # Redirect to HTTPS (Certbot will handle this)
    # RewriteEngine on
    # RewriteCond %{SERVER_NAME} =maintenance.woodsonisd.net
    # RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

# SSL configuration will be added by Certbot
# <VirtualHost *:443>
#     ServerName maintenance.woodsonisd.net
#     ServerAdmin richard.sullivan@woodsonisd.net
#
#     DocumentRoot /var/www/woodson/maintenance/public
#
#     <Directory /var/www/woodson/maintenance/public>
#         Options -Indexes +FollowSymLinks
#         AllowOverride All
#         Require all granted
#     </Directory>
#
#     ErrorLog ${APACHE_LOG_DIR}/maintenance.woodsonisd.net-error.log
#     CustomLog ${APACHE_LOG_DIR}/maintenance.woodsonisd.net-access.log combined
#
#     # SSL certificates managed by Certbot
#     SSLCertificateFile /etc/letsencrypt/live/maintenance.woodsonisd.net/fullchain.pem
#     SSLCertificateKeyFile /etc/letsencrypt/live/maintenance.woodsonisd.net/privkey.pem
#     Include /etc/letsencrypt/options-ssl-apache.conf
# </VirtualHost>
